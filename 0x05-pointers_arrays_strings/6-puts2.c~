#include "main.h"


/**
 * puts2 - Put every other charachter
 *
 * @str: string
 *
 * Return: Nothing
 */

void puts2(char *str)
{
	int i = 0;
	if (*str)
	{
		while (*(str + i) != '\0')
		{
			if (i % 2 == 0)
			{
				_putchar(str[i]);
			}
			i++;
		}
	}
}
